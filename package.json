{
  "name": "lptts",
  "version": "0.1.0",
  "private": true,
  "description": "A lightweight, private-hand 2D card table with TTS deck import.",
  "type": "module",
  "scripts": {
    "start": "node server/index.js",
    "dev": "node --watch server/index.js",
    "test": "node --test",
    "check": "node --check server/index.js && node --check public/js/app.js && node --test"
  },
  "dependencies": {
    "ws": "^8.18.3"
  },
  "engines": {
    "node": ">=20"
  }
}
